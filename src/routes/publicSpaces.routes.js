const { Router } = require('express');

const router = Router();

// Este archivo está vacío por ahora, pero necesita exportar un router válido
// para evitar errores en app.js

module.exports = router;
